<!-- ABILITIES -->
<div class="ability-scores section-container">
    <div class="section-header">
        <h4>{{localize "CO.label.long.modifiers"}}</h4>
    </div>
    <!-- ABILITY MODIFIERS -->
    <div class="section-body">
        <div class="list">
            {{#each abilities as |ability id|}}
                <div class="labelled-field" data-attribute="str">
                    <label class="rollable"><a>{{localize (concat "CO.abilities.short." id)}} {{#if ability.superior}}*{{/if}}</a></label>
                    {{#if ../unlocked}}
                        <label class="checkbox"> <input type="checkbox" name="system.abilities.{{id}}.superior" {{checked ability.superior}}></label>
                        <input class="modifier" type="text" name="system.abilities.{{id}}.mod" value="{{numberFormat ability.mod decimals=0 sign=true}}" data-dtype="Number" placeholder="0">
                    {{else}}
                        <input class="modifier" type="text" name="system.abilities.{{id}}.mod" readonly="true" value="{{numberFormat ability.mod decimals=0 sign=true}}" data-dtype="Number" placeholder="0">
                    {{/if}}
                </div>
            {{/each}}
        </div>
    </div>
</div>
<!-- ATTACKS -->
<div class="attacks section-container">
    <div class="section-header">
        <h4>{{localize "CO.label.long.attacks"}}</h4>
        <div class="attacks-controls controls">
            <a class="item-control item-create" data-type="attack" title="{{localize 'CO.label.long.add'}}"><i class="fas fa-plus"></i></a>
        </div>
    </div>
    <div class="section-body">
        <ol class="attacks-list strikes-list item-list">
            {{#each attacks as |attack id|}}
                {{log "Chroniques Oubliées | attack" attack id}}
                <!--                <div class="labelled-field" data-attribute="str">-->
                <!--                    <label class="rollable"><a>{{!localize (concat "CO.abilities.short." id)}} {{!#if ability.superior}}*{{!/if}}</a></label>-->
                <!--                    <input class="modifier" type="text" name="system.abilities.{{!id}}.mod" value="{{!numberFormat ability.mod decimals=0 sign=true}}" data-dtype="Number" placeholder="0">-->
                <!--                </div>-->
                <li class="item attack flexrow" data-item-type="attack" data-item-id="{{attack._id}}" draggable="false">
                    <div class="attack-header">
                        <h4 class="attack-name tags">
                            {{#if (eq attack.system.subtype "MELEE")}}
                                <span><strong>{{localize "CO.label.short.melee"}}</strong><span class="activity-icon"><i class="fas fa-fw fa-sword"></i></span>{{attack.name}}</span>
                            {{/if}}
                            {{#if (eq attack.system.subtype "RANGED")}}
                                <span><strong>{{localize "CO.label.short.ranged"}}</strong><span class="activity-icon"><i class="fas fa-fw fa-bow-arrow"></i></span>{{attack.name}}</span>
                            {{/if}}
                            {{#if (eq attack.system.subtype "MAGICAL")}}
                                <span><strong>{{localize "CO.label.short.magic"}}</strong><span class="activity-icon"><i class="fas fa-fw fa-wand-magic-sparkles"></i></span>{{attack.name}}</span>
                            {{/if}}
                        </h4>
                        <div class="attack-controls controls">
                            <a class="item-chat chat" title="{{localize 'CO.label.long.sendToChat'}}" data-chat-type="item"><i class="fas fa-fw fa-comment-alt"></i></a>
                            {{#if ../unlocked}}
                                <a class="item-edit edit" title="{{localize 'CO.label.long.edit'}}"><i class="fas fa-fw fa-edit"></i></a>
                                <a class="item-delete delete" title="{{localize CO.label.long.delete}}"><i class="fas fa-fw fa-trash"></i></a>
                            {{/if}}
                        </div>
                    </div>
                    <div class="attack-body">
                        <div class="flexrow">
                            <div class="attacks sub-section">
                                <button type="button" class="btn attack-button toggle-action" title="" data-action="activate" data-type="attack" data-source="{{attack.system.displayValues.source}}" data-indice="0">{{localize "CO.label.long.attack"}} {{attack.system.displayValues.attack}}</button>
                            </div>
                            <div class="damage sub-section">
                                <button type="button" class="damage-button tooltipstered toggle-action" data-action="activate" data-type="damage" data-source="{{attack.system.displayValues.source}}" data-indice="0">{{localize "CO.label.long.damage"}} {{attack.system.displayValues.damage}}</button>
                            </div>
                        </div>
                        <div class="attack-detail sub-section expandable expanded">
                            <div class="attack-description"></div>
                        </div>
                    </div>
                </li>
            {{/each}}
        </ol>
    </div>
</div>
<!-- ACTIONS -->
<div class="actions section-container">
    <div class="section-header">
        <h4>{{localize "CO.ui.actions"}}</h4>
        <div class="actions-controls controls">
            <!--            <a class="action-add item-create" title="Add Action" data-type="action" data-action-type="action"><i class="fas fa-plus"></i></a>-->
        </div>
    </div>
    <!-- ACTIONS -->
    <div class="section-body-no-flex">
        <ol class="actions-container items-container">
            <ol class="item-list">
                {{#each visibleActivableActions as |action id|}}
                    {{!log (concat "Chroniques Oubliées | action " id) action}}
                    <li class="item flexrow" draggable="true" {{#if action.properties.temporary}}{{#unless action.properties.enabled}}style="opacity: 60%" {{/unless}} {{/if}}>
                        <div class="item-name flexrow">
                            <div class="item-image" style="background-image: url('{{getActionImg action @root.actor source}}')"></div>
                            <h4><a class="item-control item-edit">{{getEmbeddedItemName @root.actor source}} : {{action.label}}</a>&nbsp;{{{getActionIcon action}}}</h4>
                        </div>
                        <div class="item-controls-3">
                            {{#if action.properties.activable}}
                                {{#if action.properties.temporary}}
                                    <a class="item-control item-activate">
                                        {{#if action.properties.enabled}}
                                            <i class="fas fa-dice-d20 green toggle-action" style="font-size:16px" title="Désactiver" data-action="unactivate" data-source="{{action.source}}" data-indice="{{action.indice}}"></i>
                                        {{else}}
                                            <i class="fas fa-dice-d20 gray toggle-action" style="font-size:16px" title="Activer" data-action="activate" data-source="{{action.source}}" data-indice="{{action.indice}}"></i>
                                        {{/if}}
                                    </a>
                                {{else}}
                                    {{#if (or (eq action.type 'melee') (eq action.type 'ranged'))}}
                                        <a class="item-control">
                                            <i class="fas fa-dice-d20 green toggle-action" style="font-size:16px" title="Attaque" data-action="activate" data-type="attack" data-source="{{action.source}}" data-indice="{{action.indice}}"></i>
                                        </a>
                                        <a class="item-control">
                                            <i class="fas fa-dice-d20 green toggle-action" style="font-size:16px" title="Dommages" data-action="activate" data-type="damage" data-source="{{action.source}}" data-indice="{{action.indice}}"></i>
                                        </a>
                                    {{else}}
                                        <a class="item-control">
                                            <i class="fa-solid green fa-sword toggle-action" style="font-size:16px" title="Utiliser" data-action="activate" data-source="{{action.source}}" data-indice="{{action.indice}}"></i>
                                        </a>
                                    {{/if}}
                                {{/if}}
                            {{/if}}
                            <a class="item-chat chat" title="localize CO.label.long.sendToChat" data-chat-type="action" data-indice="{{action.indice}}" data-source="{{action.source}}"><i class="fas fa-fw fa-comment-alt"></i></a>
                        </div>
                    </li>
                {{/each}}
            </ol>
        </ol>
    </div>
</div>