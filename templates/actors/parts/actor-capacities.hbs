{{!log "Chroniques Oubliées | character capacities" this}}
<ol class="features items-container" data-tab="features">
    <li class="items-container-header flexrow">
        <h3 class="item-name flexrow">
            <a class="section-toggle">{{localize "CO.ui.capacitiesOffPaths"}}</a>
        </h3>
        <div class="item-controls-2">
            <a class="item-control item-create" data-type="capacity" data-tooltip="{{localize 'CO.label.long.add'}}"><i class="fas fa-plus"></i></a>
        </div>
    </li>
    <ol class="item-list foldable">
        {{#each capacitiesOffPaths as |capacity id|}}
            {{!log "Chroniques Oubliées | capacity" capacity}}
            {{#if capacity.system.learned }}
            {{#if (isNull capacity.system.parentCapacity)}}
                <li class="item flexrow" data-item-uuid="{{capacity.uuid}}" data-item-id="{{capacity._id}}" data-item-type="capacity" draggable="true">
                    <div class="item-name flexrow">
                        <a class="item-img item-edit" style="background-image: url('{{capacity.img}}')"></a>
                        <h4>{{capacity.name}}{{#if capacity.system.isSpell}} * {{/if}}{{#if capacity.system.hasActionType}} ({{capacity.system.actionTypeShort}}){{/if}}</h4>
                    </div>
                    <div class="item-controls-3">
                        <a class="item-chat chat" data-tooltip="{{localize 'CO.label.long.sendToChat'}}" data-chat-type="item"><i class="fas fa-fw fa-comment-alt"></i></a>
                        {{#if ../unlocked}}
                            <a class="item-edit edit" data-tooltip="{{localize 'CO.label.long.edit'}}"><i class="fas fa-fw fa-edit"></i></a>
                            <a class="item-control item-delete" data-tooltip="{{localize 'CO.ui.remove'}}"><i class="fas fa-trash"></i></a>
                        {{/if}}
                    </div>
                </li>
                {{/if}}
            {{/if}}
        {{/each}}
    </ol>
</ol>
