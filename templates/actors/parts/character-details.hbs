{{! log 'detail' this}}
<div class="flexrow top">
    <div class="flex1" style="margin-right: 5px;">

        <div class="flexrow">
            <div class="flex1 center bg-header">
                <span class="field-label">{{ localize 'CO.label.name' }}</span>
            </div>
            <div class="flex2 center cell">
                <input class="field-value sans" name="name" type="text" value="{{actor.name}}" placeholder="Name" data-dtype="String" />
            </div>
        </div>

        <div class="flexrow">
            <div class="flex1 center bg-header">
                <a class="flex2 levelUp">{{ localize 'CO.label.level' }}</a>
            </div>
            <div class="flex2 center cell">
                <input class="field-value" name="system.xp.level.value" value="{{xp.level.value}}" data-dtype="Number" />
            </div>
        </div>
    </div>
    <!--
    <div class="flex1">

        <div class="flexrow">
            <div class="flex1 center bg-header">
                <span class="field-label">{{ localize 'CO.label.specie' }}</span>
            </div>
            {{#if species}}
                {{#with species}}
                    <div class="flex2 center cell drop-field cof-compendium-pack item" data-item-id="{{_id}}" data-item-type="species" data-pack="species" data-open="0" data-tooltip="{{ localize 'COF.species.openCompendium' }}">                
                        <div class="flex5 field-value sans item-edit" style="background-image: url('{{img}}'); background-size: 26px 26px; background-repeat: no-repeat">{{name}}</div>
                        <div class="species-controls item" data-item-id="{{_id}}" style="padding-right:5px;">
                            <a class="item-control item-delete" data-tooltip="{{localize 'CO.label.long.deleteItem'}}"><i class="fas fa-times"></i></a>
                        </div>                
                    </div>
                {{/with}}
            {{else}}
                <div class="flex2 center cell drop-field cof-compendium-pack" data-item-type="species" data-pack="species" data-open="0" data-tooltip="{{localize 'COF.ui.openCompendium'}}"></div>
            {{/if}}
        </div>


            <div class="flexrow">
                <div class="flex2 center bg-header">
                    <span class="field-label">{{ localize 'CO.label.profile' }}</span>
                </div>
                {{#if profile}}
                    {{#with profile}}
                        <div class="flex4 center cell drop-field cof-compendium-pack item" data-item-id="{{_id}}" data-item-type="profile" data-pack="profiles" data-open="0" data-tooltip="{{ localize 'COF.path.openCompendium' }}">                
                            <div class="flex5 field-value sans item-edit" style="background-image: url('{{img}}'); background-size: 26px 26px; background-repeat: no-repeat">{{name}}</div>
                            <div class="profile-controls item" data-item-id="{{_id}}" style="padding-right:5px;">
                                <a class="item-control item-delete" data-tooltip="{{localize 'CO.label.long.deleteItem'}}"><i class="fas fa-times"></i></a>
                            </div>                
                        </div>
                    {{/with}}
                {{else}}
                    <div class="flex4 center cell drop-field cof-compendium-pack" data-item-type="profile" data-pack="profiles" data-open="0" data-tooltip="{{localize 'COF.ui.openCompendium'}}"></div>
                {{/if}}
            </div>
        
    </div>
    -->
</div>